export default interface IBadgeReq {
    id: string;
    user: string;
    badge: string;
    object:string;
    comment: string;
    proof: string;
}



/*
*
* All
* Une seule via id
* Insert
* Update
* Delete
*
* */
